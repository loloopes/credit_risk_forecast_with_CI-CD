.vscode
# Dataset files live in DVC; Git tracks only *.dvc pointers (see `dvc add data`)
/data/**
!/data/**/*.dvc
.work-folder-info
.vfsmeta
.stubs
.vfscache
minio_data
postgres_data
.env
.github/secrets/airflow-secrets.env

# PySpark on Windows: auto-downloaded winutils (see lgbm_test.ipynb)
.spark-hadoop/

# Airflow local / bind-mounted task logs (dag_id=…/run_id=…/attempt=…)
logs/
*.pkl
__pycache__/
*.py[cod]
*.pyc
/data
